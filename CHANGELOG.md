# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。日期格式为 `YYYY-MM-DD`。

## [1.0.4] — 2026-09-18

### 修复了部分视听练习的问题，优化了「音阶填空」的逻辑

- **口径整体反转**：现在是**默认把参考音铺在指板上**（做题的依据），要你填的那几个位置
  **默认没有任何标记** —— 得自己按音阶结构推出来。此前正好相反：参考音要点了「提示」才给，
  待填的位置反而被提前圈好，对着圈点就能答。
- **「提示」改成指向答案的位置**：它只做一件事 —— **把待填的位置标出来**，
  而且是**两态开关**，可以来回切。开不开提示，**参考音一个都不变**。
- **题目不再改写全局状态**：出题时临时用到的调性、存档与探索页状态全部收回题内，
  换题 / 停局不再污染顶栏调性与本机偏好；题目标题里的调名也跟着升降号设置走
  （此前硬写升号）。
- **题目一定点得出来**：目标音按当前**弦组**过滤（此前会出现「要你点的音根本不在显示的弦上」）。
- **不再误判**：「已显示的音阶音」不再算作答对；可填位置数取满时不再顶穿可点位置数。
- **「指板范围」在练习页就能改**：与「指板探索」「指板练习」共用同一份设置、双向同步；
  练习途中换品段会**作废当前题并重出一题**，不会留下按旧范围出的题。
- **分组设置统一**：听辨 / 视奏 / 填空三种题型共用同一套**母音阶 → 子音阶组**卡片，
  可任意多选，不必再按预设档位出题。

### 更新了「指板练习」：模块化的指板练习定制

- **新增「指板练习」栏目**（标签栏第 2 位）；原「练习模式」改名为「**视听练习**」。
- **按定弦自动切两套组织逻辑**：吉他 6 / 7 弦的纯四度、标准调弦、全降半音 / 全降全音这几族，
  模进编排**以音程关系为主、位置关系为辅**；其他开放调弦（DADGAD / Open G / Open D /
  FACGCE / DAEAC#E / CGDGBD…）则**按把位逻辑**优化。
- **练习级别 L0–L4**（逐音 / 二音组 / 三音组 / 四音组 / 套嵌），模进布局与音序 σ 都可选。
  三音组是枚举能力最强的一档：长度 ≤ 4 个音的走句里它能给出 8 种音序，四音组只有 6 种。
- **位置选择器**：可以只练某个指型系统的某个把位，也可以多选、甚至**跨指型系统混选**
  （五声族 76 处、六 / 七 / 八声族各 102 处可选）。收起式摘要行 + 弹出面板；
  选中的位置进一个可拖动的**练习顺序槽**；段序两档可切（默认按品序）；
  **换把区在指板图上自动标注**。
- **逐块串行**：一个把位块走完才换下一块。指板图下方**每个块都有一个按钮**，点一下换下一档方向；
  也可以**在指板图上右键**那一块，八档菜单任选，还能「全部同此」。
- **走句方向从 3 档扩到 8 档**：上行右 / 上行左 / 下行左 / 下行右 / 上下行 / 下上行 /
  智能决定 / 跟随工具条。四档「连接」是给「一块接一块练」用的，差别只看**最后一个音落在哪**：

  | 档 | 走到哪、怎么收 | 末音落在 |
  |---|---|---|
  | 上行右连接 | 走到最高音弦的最高音就收住（＝原来的「上行」） | 右上角 |
  | 上行左连接 | 升到顶后**在最高音弦上折回** | **最高音弦的最低音** —— 接左边（低把位）那一块 |
  | 下行左连接 | 走到最低音弦的最低音就收住（＝原来的「下行」） | 左下角 |
  | 下行右连接 | 到底后**在最低音弦上折回** | **最低音弦的最高音** —— 接右边（高把位）那一块 |
  | 下上行 | 上下行的镜像：先倒着走到最低音，再原路回到最高音 | 顶点 |

  - 按钮副标直接写着这一块走句**真正的末音**（「收在 X 弦 Y 品」），点一下就能看出接的是哪一块。
- **「几音一组」为什么不够用**：模进那一层只认「一条线 + 整段变换」，而
  「末音必须落在某根弦的某个品」是**坐标相关**的条件 —— 音序 σ 决定末音（末音未必落在窗口最后一格），
  套嵌档还会把单元整体重排。所以收尾做成**模进整段走完、再追加一段独立的连接尾**，
  模进滑窗那一层**一行未改**。全枚举 **13,032 次「块 × 档」**走句实测：连接档的末音
  **100%** 落在对应的物理极值弦上，且不引入任何新的大跳。

### 更新了指型谱（TAB + 五线谱）与循环播放，方便跟练

- **每个指板图下方都附一条指型谱**（「不分区」模式除外）：**TAB 谱 + 五线谱**，
  从该图**最低音弦的最低音**起、逐弦升品走到**最高音弦的最高音**，并支持反向（下行）与上下行。
- **TAB 类型跟「乐器 + 定弦」走**：六线 / 七线 TAB 给吉他，四线 / 五线 TAB 给贝斯；
  五线谱的谱表也自动分派 —— 吉他走高音谱表，贝斯走低音谱表（F 谱号）。
  **19 条定弦逐一核查**：TAB 横线数 = 弦数（＋左端那一条琴枕栏线），五线谱按乐器出对应谱表。
- **长走句自动分页**：每行最多 **18 个音**，并且**均分**（各行最多差 1 个音）。
  分页界限只由这一条决定，与版面、乐器、定弦都无关 —— 一块再长也不会被压扁到看不清。
- **播放与跟练**：**▶ 播放**按谱面音高依次放一遍，**时值与左下角 BPM 保持一致**；
  **🔁 循环**播到最后自动从头再来。
- **播到哪一颗就只亮哪一颗**：指板图上只点亮谱面当前那一颗音**在走句里记着的那一个位置**
  （弦 + 品唯一确定一个音位），TAB 与五线谱同步高亮 —— 不再把同音高的位置全部点亮，
  跟谱时看得出手该按哪根弦哪个品。该位置这一帧恰好没画出来时（品段裁掉、分屏不在本窗），
  退回升到按音高找，不留「什么都没亮」的空拍。

## [1.0.3] — 2026-09-17

### 新增「五线谱视奏」题型：看谱在指板上找音

- **练习模式新增第 6 个题型**（练习模式现共 6 个）。按**母音阶组**出题，与「音阶听辨」共用
  那 8 组分组；**每题音符数自己定**（3–12，默认 5）。勾选、音符数与音名开关都存进本机偏好。
- **题目画成五线谱**（高音谱表）：带**调号**、带**节奏** —— 时值、符干、符尾、附点、休止符
  都按真实记谱画，一小节一小节排下去，收尾给一条终止小节线。
  七声调式按所属大调自动给调号；五声 / 布鲁斯这类没有标准调号的按「临时记号最少」挑一个，
  实在不合适就不给调号，音阶外的音一律写成临时记号。**谱面按吉他习惯高八度记谱**。
- **题目一定点得出来**：出题只在「当前可点范围里属于本题音阶的音」中走句；
  范围收窄到装不满时题板会明写一句提示，不静默少音。
- **读谱辅助**：**▶ 试听题目**按谱面音高依次放一遍（休止符只占时间不出声）；
  **☐ 显示音名**把每个音的音名标在谱下。
- **按谱面顺序依次点出**：点一下，**指板上所有同音高的位置同时高亮**（环色是该音高类的专用色，
  与「和弦查询」同一套 12 色），顺便看清这个音在琴颈上的所有落点。
- 指板图下方实时记录**你的作答**（带序号），支持 **↶ 撤回上一步** 与**清空作答**，
  点满后按 **✅ 提交答案**。
- **判定卡八度** —— 谱面写的是哪个音高就点出那个音高：同一个音高换个把位算对
  （1 弦空弦和 2 弦 5 品都是 E4），点成别的八度算错。
  全对自动进下一题并加分；有错则谱面上那几颗标红叉、扣 6 分、**停在本题**，撤回改对再提交即可。

### 视奏练习支持贝斯定弦：题目画在低音谱表上

- **谱表跟定弦自动走** —— 顶栏选吉他（含各种开放定弦）时题目画在**高音谱表**，
  选**贝斯 4 弦 EADG / 贝斯 5 弦 BEADG** 时改画在**低音谱表**（F 谱号）。不新增开关，
  也不用自己切。**同一族内换定弦**（4 弦 ↔ 5 弦）保留当前题，**跨族换定弦**自动换一题。
- **记谱按贝斯谱惯例**：谱面音高 = 实际发声 + 12（low E 发声 E1，谱上写在低音谱表下加一线），
  与吉他的高八度记谱平行一致。谱下「显示音名」标的是**谱面音名**。
- **贝斯题只在低音谱表的常用音域里走句**：谱位限制在**加线不超过 3 条**那一段
  （约等于实际发声 A0–G3，下限由定弦决定，上限落在 G 弦 12 品）。
  若按指板范围直走到 24 品，谱面会拖出七八条加线，那样的谱没法读。
  收窄到这个窗仍然不够用时，题板照旧明写一句提示（贝斯题会多一句说明）。
- **调号按所用谱表排**：低音谱表上各升降号落在它该在的谱位上（与高音谱表是两套位置）。
- 内部把「谱表」抽成配置（`STAFF_TREBLE` / `STAFF_BASS`）、把「声部」抽成配置
  （谱表 + 记谱八度 + 音域窗），出题内核吉他与贝斯共用 —— **高音谱表的渲染结果逐字不变**。

### 修复：贝斯视奏题板的音符画低了一个八度

- 谱表配置化时，符头中心 / 加线 / 附点三处的「谱位 → 纵向位置」换算漏带了谱表参数，
  被按高音谱表换算 —— 两张谱表相差整整一个八度，于是**贝斯题整条走句掉到谱表下方六格、
  还挂出一串多余的加线**。谱号 / 调号 / 五线 / 拍号 / 休止符 / 小节线当时都是对的，
  只看这些会以为画对了。
- 现已修正：符头 / 加线 / 附点各自按**当前谱表**换算。贝斯题板的高度随之从 147 收到 86
  （同样一道题，谱面不再有多余空白）。顺带把拍号也纳进了画布上下沿的计算。
- 高音谱表（吉他）的渲染结果**逐字未变**。

### 和弦查询兼容「只点了 2 个音」的情况

- **只点 2 个音时不再只说「还差 1 个音」**，改成给出两条结果，各自明确写出：
  **音程关系**（低音 → 高音的两个音名、音程名与国际缩写、半音数，并附一条反向读数 —— 互为转位）
  与**特殊和弦**（这两个音是否凑成一个「构成音天生只有两个」的和弦）。
- **两音和弦只认五和弦**（强力和弦 `X5`＝根音 ＋ 纯五度），且**两种摆法都认**：
  7 个半音写原位 `X5`，5 个半音写转位 `X5/Y`。其余 9 个音程写「不适用」并说明原因，不硬凑名字。
- **方向按实际音高定**（低音 ＝ 已选里音高最低的那个位置），与斜线和弦的记法同一口径 ——
  同一对音换个摆法不会给出两个打架的说法。
- 该逻辑**只在恰好 2 个音时触发**：3 个音起仍走原来的粗匹配 / 音级匹配 / 调内和弦；
  1 个音时仍提示还差几个音，只多一句「点满 2 个音可以先看这两颗音的音程关系」。
- 和弦查询页「匹配方式」卡片的提示同步为「匹配最少 3 个音 · 点 2 个音看音程」。

## [1.0.2] — 2026-09-16

### 修正「调内和弦」的体系下拉：只留五个七声体系，并修掉「后面几个选不上」

- **体系下拉只保留五个七声体系** —— 自然大调 / 和声小调 / 旋律小调 / 和声大调 / 双和声小调。
  五声与布鲁斯、对称音阶、异域与 Bebop、琶音都谈不上「这个调下的各级和弦」，不再出现在这里。
- **修掉「除自然大调体系外，其它体系一个都选不上」**：原来把**音阶 id** 当**组 id**
  去查 `scalesOf()`，查询恒空、每次回落 `major`，顺手把刚选的体系也冲掉。
  现在体系与母音阶是两个状态（`CQ.pgrp` / `CQ.psc`），由 `cqDiaNorm()` 保证两者始终自洽；
  两个下拉的选项出自同一个 `cqGroupOptsHTML()`，不会再出现一处改了另一处忘。
- **双和声小调族里的非常规三度叠置不再被说成大三和弦**：那条音阶上三度叠置会给出
  `1 3 b5`（`0·4·6`）这类音组，罗马数字改为「小写 + 问号」，行内直接写出它的音程。

### 优化「点音反查」的交互与配色

- **点完音立刻出结果** —— 以前点完要在页面上再碰一下（切页 / 换模式 / 动下拉）才刷新结果区。
- **12 个音高类各一种颜色**（色相按五度圈排），不再只有「低音红 / 其余蓝」两种色；
  **低音改用虚线环**标记。左侧已选音列表每行加同色色点，与指板一一对应。
- 已选音列表补上**全部位置**（几弦几品）、**单个删除**的 `×` 按钮，
  以及「范围外」角标（不在当前显示范围里的位置仍照常参与匹配）。
- 点音上限改为按**音高类**卡（12 个）而不是位置数，超限给提示、不再静默丢掉最早点的音；
  位置数另设 24 的软上限。
- 门槛提示改成「还差 N 个音」，并区分「位置数」与「音高类数」；
  「匹配结果」标题右侧补上命中条数（命中 0 条时写「无命中」）。


### 新增「🎯 和弦查询」与「🎼 音阶查询」两个栏目

顶栏多了两个**反查**入口：以前是「选音阶 → 看指板」，现在是「在指板上点音 → 反推和弦 / 音阶」。
两页共用一块可点的指板（点一下选中、再点一下取消，可撤销 / 清空 / 试听已选音）。

**和弦查询（最少 3 个音）**，三种匹配方式：

- **粗匹配** —— 只知道按到了哪些音。枚举 12 个候选根音 × 46 条和弦性质
  （比「即兴查询表」宽一档：补齐六和弦、九 / 十一 / 十三、变化属，以及各种**缺音形态**），
  按「精确命中 / 加音读法 / 少音读法」三类给出结果。
- **音级匹配** —— 给每颗音标上它相对和弦根音是**几音**（不确定就留「不知道」）。
  有几颗已知就够收敛；根音没被指定时结果里会出现**斜线和弦** `X/Y`。
- **调内和弦** —— 先说清处在哪个母音阶下，只在该音阶的七级和弦里找，
  并按「常见的可能性」从高到低排；再给某几颗音标上**它在这个和弦里是第几个音**，
  就从粗匹配进到细匹配。

匹配只看**音高类**（同一个音在高音弦、低音弦各点一次只算一个音）；
**低音取已选里音高最低的那个位置**（不是先点的那个），斜线和弦的 `Y` 就是它。

**和弦「可能性」是从本机语料数出来的**，不是拍脑袋：把教材进行集里每颗和弦根音按
「相对主音的半音数」计数，归一化后大调七级顺序实测为 **V > I > IV > vi > ii > iii > vii°**
（偏移 7 出现 171 次、偏移 0 出现 138 次 —— 两个主力咬得很紧）。
只数教材进行集、不数经典进行集：后者几乎全落在大调体系内，
加权之后会把 V 抬到 I 前面（实测 `7:203 > 0:178`），排序就错了。

**对不上任何和弦时**，页面按那套「去掉七声音阶的根音、拆成两个三音组」的体系兜底分类 ——
三和弦 / 四度叠置 / 音簇 / 七和弦缺五音 / 七和弦缺三音，五族各 4 个成员
（C 伊奥尼亚去掉根音后 6 个音取三 = 20 颗 = 5 族 × 4）。
这颗三音组归哪一族、能和哪一族配成完整的一组，都列出来。

**音阶查询（最少 5 个音）**：

- **粗匹配** —— 在 82 条音阶 × 12 个主音的 **984 条读法**里找「装得下全部点出音」的音阶，
  按「你还差几个音」分层（点 5 个音、命中七声音阶 = 还差 2 个）。
- **细匹配** —— 指定主音后收敛。

**同一组音往往有好几个名字**，所以一条结果 = **一组音高类**，不是一个音阶名：
七声音阶的同一组音有 7 个读法（C 伊奥尼亚 = D 多利亚 = …），五声音阶有 5 个
（C 大调五声 = A 小调五声），对称音阶更极端（全音阶转 2 / 4 / 6 / 8 / 10 个半音都还是它自己）。
结果里把**同一组音的全部读法并列**出来，每个都能一键当主音进细匹配；
对称的那些另有徽标记明「连主音都分不出来，只能给出母音阶组」。

判据用的是「点出的音**能同时全部放进**这个音阶」，比「点出音之间的每个音程都能在音阶里找到」
更严 —— 后者会把 `{C, Eb, G}` 错收进 C 大调（3、4、7 三个音程 C 大调里都有，但 `Eb` 根本不在里面）。

**其余一切不变**：与上一轮最终态逐块比对，385 个老代码块、10 个老页面段落、350 条老 CSS 规则
**一字不变**；改动的只有 `switchPage`（加两条新页路由）、`boot`（多一行把新页的事件挂上）
与 `initDove`（前面插了一段注释）。

### Bebop 音阶的指型编排

- **三条 Bebop 音阶（大比博普 / 多利亚比博普 / 属比博普）的九大指型改用「母音阶骨架 +
  把位内点缀经过音」编排**。它们比各自的母音阶多**一颗经过音**、共 8 个音，
  此前是**按这 8 个音独立重排指型** —— 结果把母音阶的 7 个把位丢成 8 个、手型也认不出来；
  在 7 弦 C# 定弦（`C#F#BEG#C#C#`）上甚至还凭空多出 4NPS 的一块，
  而那条定弦对**任何**七声音阶都铺不出 4NPS。
  现在以母音阶（`ionian` / `dorian` / `mixolydian`）的对应指型为**骨架**，
  再把落在本把位品窗内的经过音**点缀**进去：它紧跟在下方邻音之后
  （属比博普 `b7`、大比博普 `5`、多利亚比博普 `b3` —— 三条上恰好都是和弦音，
  所以三度叠置的 2-1-2 琶音指型也接得住）。越出本把位品窗的不点，
  骨架里没有那颗下邻音的不点。
- **效果**：三条 Bebop 音阶的**块数与母音阶逐组相等** —— 7 个把位与手型原样保住，
  57144 块 / 74109 颗经过音全部落在 7 品窗内；7 弦 C# 定弦上不再出现那个假的 4NPS 档
  （全库 4NPS 组数 470 → 467）。
- **页面新增说明**：选到这三条音阶时，指型提示行会追一句「以母音阶『××』的指型为骨架、
  本把位内点缀 ×× 经过音（紧接在 ×× 之后）」，音阶库里也补一段同样的说明段。
- **其余一切逐块不变**：全量回归逐行比对，非 bebop 音阶 57152 块 → 57152 块，
  **预期外差异 0 组、凭空消失 0 组**。

### 「宽指型」替换 4NPS（整个「五声 / 布鲁斯」族）+ 新增属五声音阶

- **「五声 / 布鲁斯」族全 12 条的「一弦四音 4NPS」档，一律换成「宽指型」**
  （第 46 轮定稿为「本把位 7 品窗全取 · 一弦 3–4 音」，见下一节）。分三次铺开：
  第 41 轮大调 / 小调五声；第 44 轮扩到 9 条 5 音五声（平调子 / 阴旋法 / 云井调 / 埃及音阶 /
  五声调式 3・4 型，判据改成按**音数**判）；第 45 轮扩到 3 条 6 音布鲁斯族
  （判据再改成按**族**判，只管 `g === 'pent'`）。
  理由是全族一致的实测事实 —— **标准定弦上这 12 条的 4NPS 一块都画不出来**：
  5 音音阶是每弦 4 音 × 6 弦 = 24 音 ≈ 4.8 个八度，远远超出 24 品；
  6 音的布鲁斯族正好 24 音 = 4 个八度，恰好等于整把琴的音域，
  「每根弦都恰好贡献 8 个半音」这个条件无解（它们只在 `bass4` / `bass5` / `dropd` /
  `fourths` / `daeacse` 几条定弦上零星出块，`dadgad` / `g7std` 上同样是 0）。
- **新增「属五声音阶」**（`pent_dom`，`1-3-4-5-b7`）到「五声 / 布鲁斯」组。小调五声把 `b3`
  升成 `3` 就是它，等价说法是混合利底亚去掉 2 和 6 —— 属七的四个和弦音一个不落、
  只多一颗 `4`，从熟的小调五声乐句换过来只需挪一颗音。文献里它也叫**印度（印度教）
  五声音阶**。特征和弦取 `7`，并已补进「即兴查询表」的属七族候选。
- 计数：音阶 / 琶音条目 **81 → 82**，调式色彩档案 **58 → 59**。
- **6 音布鲁斯族（第 45 轮）**：它们**走的是同一条规则** —— 一个 7 品把位窗里、每根弦上的
  全部音阶音。5 音音阶在这个窗里自然落成每弦 3 个（看着像一弦三音），6 音布鲁斯族落成 3–4 个。
  每弦 4 音在 6 音音阶上正是那条铺不出来的路，所以这一族同样只能走「宽指型」。
  （第 45 轮当时是按「两个相邻把位取并集 ＋ 每弦取 3 个」实现的，第 46 轮已改正。）
- **改名**：用户可见名字从「宽五声」改成「**宽指型**」—— 它现在覆盖 6 音的布鲁斯族，
  再叫五声就名不副实。内部 id 仍为 `pentwide`。
- **换档结果（第 44 轮）**：那 6 条音阶改后「一弦四音」的行**全部消失**
  （此前它们在整个定弦表上只有 3 条各出过 2 块），宽指型在**全部 17 条定弦**上每块都铺满
  5 组、每组 5 块。
- **换档结果（第 45 轮）**：3 条布鲁斯族的「4nps」行全部消失（改前各有 5 / 5 / 6 条定弦出块），
  「宽指型」在**全部 17 条定弦**上各出 **6 组、每组 6 块**（锚点数 = 6 个音）。
- **其余一切逐块不变**：两轮各自做了改前 / 改后逐组回归比对（五声族 + 大调 7 个调式作对照组）：
  第 44 轮 **2299 → 2533 组**、第 45 轮 **2533 → 2568 组**，两轮都是
  **非靶子音阶预期外差异 0 组、凭空消失 0 组**；对照组 945 组逐行零差异；
  第 44 轮点名不动的 5 条、第 45 轮点名不动的 9 条五声，全部**逐行完全不变**。

### 「宽指型」改成「本把位 7 品窗全取」

- **改正铺法**：宽指型的一块 = **一个 7 品把位窗里、每根弦上的全部音阶音**。窗取
  `[锚点品 − d, 锚点品 − d + 6]`，`d` 取**最小的、让每根弦都至少有 3 个音阶音**的值
  （最多让 4 品）；让不到就取「完整弦数最多、其次窗内音数最多」的那个窗。
  此前是「两个相邻把位（各每弦 2 音）取并集，要求每弦恰 3 音」，并集不成立时退回
  「从本把位每弦最低音起取 3 个连续音阶音」—— **两条腿都把「每弦恰好 3 音」当成了定义**，
  于是出现两个可实测的毛病：**① 砍掉窗里本来就有的第 4 个音**（C 布鲁斯第 6 弦的 `Bb@6`、
  第 4 弦的 `G@5`）；**② 把音铺到把位窗之外**（第 3 弦的 `Eb@8`、第 2 弦的 `Gb@7`
  落在窗 `[0,6]` 以外）。
- **改动来自两张指板图**（C 布鲁斯 / 标准定弦 / 主音 C）：锚点 `F@1` → 窗 `[0–6]`，
  逐弦 `1,2,3,6` / `1,3,6` / `1,3,4,5` / `0,3,5` / `1,4,6` / `1,2,3,6`；
  锚点 `G@3` → 窗 `[3–9]`，逐弦 `3,6,8` / `3,6,8,9` / `3,4,5,8` / `3,5,8` / `4,6,7,8` / `3,6,8`。
  两张图都是「窗内全部音阶音」，**每弦不恒定 3 个**（图 1 是 4/3/3/4/3/4，图 2 是 3/4/3/4/3/3）。
  按这两张图倒推出来的就是上面那条规则 —— 在规则定弦 ＋ 5 音音阶上，它与旧的并集铺法
  **逐音相同**，所以这是旧行为的自然推广，不是另起一套。
- **实测**（`p46img.js`，五组判据，**FAIL 0 / 1092 项检查**）：两张图逐弦逐品全中；
  全族 12 条 × 17 定弦 = **1071 块**与**独立手写**的参考实现逐音一致，锚点数 = 块数
  （**没有一块被丢掉**）；几何不变量全成立 —— 窗宽恒 **7 品**、音全在窗内、每弦 ≥1 音、
  **含全部音级 1071/1071**；分屏视图逐图配块 6/6 命中。
  ⚠️ **「每弦 ≥3 音」有 109 处未达成**（全是「完整弦 5/6」的尽力而为窗，如
  `pent_dom/standard` 第 5 弦、`pent_major/fourths7` 第 6 弦），逐条列出 ——
  `d ≤ 4` 内不存在「每弦都够 3 音」的窗；旧规则为了凑满 3 音会把音**铺到窗外**，那才是错的。
  音级覆盖也因此不再需要旧版那条「4 弦 ＋ 6 音允许少 1 级」的例外。
- **下拉标签与提示行**：`宽指型（本把位 7 品窗全取 · 一弦 3–4 音）`；提示行改成
  「本把位 7 品窗内的全部音阶音 …… 窗 = 从锚点品往低音方向最多让 4 品、宽 7 品」，
  并且**不再拼**「五声族音阶不受 7 品窗约束」那一句 —— 宽指型本身就是靠这个 7 品窗定义的，
  两句拼在一起会自相矛盾。
- **代码清理**：`SQ2` / `lay2` 两个局部量与 `pickW` 上的宽指型分支整条删掉；
  宽指型现在只走 `wideAt`，`buildBlocks` 里的分岔只剩一行。
- **回归**（`_reg46.js` ＋ `_reg46cmp.py`）：同一探针在改前 / 改后两份文件上各跑一遍
  （只读渲染结果），按「音阶 | 定弦」逐键比「块 → 逐弦逐品」指纹，共 **969 键** ——

  | 节 | 键数 | 逐行相同 | 不同 | 只在旧 / 只在新 |
  |---|---|---|---|---|
  | 1 宽指型（全族 12 条 × 17 定弦） | 204 | 39 | **165** | 0 / 0 |
  | 2 同族 × 2NPS / 3NPS | 408 | **408** | 0 | 0 / 0 |
  | 3 对照组：大调七调式 × 3NPS / 2NPS / 3-1-3 | 357 | **357** | 0 | 0 / 0 |

  - **没有任何键凭空出现或消失**。
  - 靶子里逐行未变的 39 键**全在「弦间一律纯四度」的定弦上**，且只涉及 5 条
    **音程形态规整的 5 音音阶**（`pent_major` / `pent_minor` / `egyptian` / `pent_mode3` /
    `pent_mode4`）：`standard` / `halfdown` / `fourths` / `g7std` / `g7low` / `bass4` /
    `bass5` 各保留 5 键，`fourths7`（七弦全四度）保留 4 键。
  - 变了的都能解释成本次故意做的替换：① 同定弦上的 `pent_dom`、`hirajoshi` / `insen` /
    `kumoi` 与 3 条 6 音布鲁斯族；② 不规则定弦（`dropd` / `dadgad` / `openg` / `open_d` /
    `facgce` / `daeacse` / `cgdgbd` / `g7dropa` / `g7cs`）上的**全族 12 条**。
  - **差异方向只有一种 —— 把越窗的音收回窗内**：按「一块里所有音的品域跨度」量，
    改前 **1071 块里有 568 块跨度 > 7 品**（最宽到 **13 品**），改后 **0 块**
    （跨度分布 6 品 220 块 / 7 品 851 块）。**改前逐行相同的那 195 块，改前本来就全在
    7 品窗内** —— 没有一处是「新规则把对的改错了」。
- **文档同步**：`docs/数据与算法.md` 的「宽指型」一节整段重写（规则、影响半径、实测、回归）；
  `docs/功能说明.md` 的指型表与换档说明；两份 README 的指型摘要一行。

### 技术

- 单文件、零依赖、无构建，`index.html` **787,189 字节**，
  SHA256 `1c8154777fb4feb17313da6d12418e9262abdd2956c6fa994b52f589decf7dcc`。
  （上一版 `688,370` 字节 / SHA256 `96e9f8f3…`。）

## [1.0.1] — 2026-09-15

本版全部改动集中在「指型怎么铺」与「和弦音怎么写名字」两件事上。除下面列出的条目外，
其余定弦 / 音阶 / 指型的渲染结果**逐块不变**（全量回归逐行比对：预期外差异 0 组、凭空消失 0 组）。

### 1. CAGED 和弦查询 · 构成音标识更符合乐理表达

- **CAGED 推导指型改成「全局分配 + 分级保底」**。此前是「每根弦各自挑离骨架槽位最近的音」，
  而骨架槽位上原本全是 1 / 3 / 5，逐格取最近必然整排原地不动 —— 和弦里多出来的音
  （六和弦的 6、九和弦的 2、小大七的 7、减七的 6 ……）**一个都进不了图**。
  实测大六 / 小六和弦的五个型与大三 / 小三和弦**逐格全等**，add9、maj9 与 maj 同样全等，
  共 12 个性质 × 5 型 = **60 张卡片漏音**。
  现在按「每个槽位 × 每个音级取位移最小的那一个八度」建候选，穷举求解，
  代价 = `总位移 + 0.4×跨度 + 3.5×静音弦数 + 缺音惩罚`，
  缺音惩罚分档 **特征音 100 ≫ 根音 20 > 三音 14 > 五音 6**。
  结果自动收敛到标准手型 `E6=022120 · A6=x02222 · D6=xx0202 · G6=320000 · C6=x32210`。
- **构成音按乐理功能严格命名**。此前只有一张**音阶口径**的表（一个半音数只配一个名字），
  可同一个半音数在不同和弦里担任的功能并不相同：属七升五 / 增三里的 8 半音是「把五音升上去」，
  旧版一律写 `b6`，而页面自己的琶音描述本来就写着 `R-3-#5` —— 图上标签和文案自相矛盾。
  现在十三和弦里的 9 半音写 `13`、加九 / 九和弦族里的 2 半音写 `9`、上面那个 8 半音写 `#5`。
  判名只看两件事：**三音（3 / 4 半音）在不在**、**纯五度（7 半音）在不在**。
  **刻意没动的**：`b7` / `7` / `5` / `b3` 保持原样。
  另加「外音回表」：半音数不属于该和弦时退回音阶口径 —— 斜杠和弦的转位低音走的就是这条路，
  不会再被安上「十一音」这种和弦功能名。
- **减七和弦的第七音**的音级名从 `6` 改成 `bb7`。

### 2. 3-1-3 指型的编排

- **最高弦改为 4 个音**（跨弦 `3-1-3-1-4`、5 弦 12 音）。此前记的「3-1-3-1-3、5 弦 11 音」
  漏了最高弦多出来的那个低音。最高弦的**两个高音同时落在下一个把位里** —— 这就是换把咬合。
- 最高弦那 4 个音**换弦不跳**（其余换弦跳过一个音阶音）。
- **两条起点拆成两套**：六弦根音型与五弦根音型各用一套，更符合运指需求。
  两套在最高弦那 4 个音上**逐音相同**，换把咬合不受影响，差别只在紧邻最高弦的那根「1 音弦」。
- `3-1-3-1-4` 里第二个「1」改为**级进**。
- 「指型方式」下拉框下面那类长段说明压成一行要点。

### 3. 开放调弦下各个指型的安排

- 原先给六种开放调弦（Open D / Open G / DADGAD / FACGCE / DAEAC#E / CGDGBD）
  **各配一套**手工错位的指型。现在改成**一条统一规则**管住所有跨弦指型 ——
  开放调弦的差异**自动**体现在「这个窗里能取到哪些音」上（弦间音程不同 → 可选音不同 → 品位不同），
  「手工错位」那整套连根拔掉。
- **横向窗宽目标 7 品**（此前是 6 品）：跨弦指型的把位左右控制在 `WIN = 7` 品内
  （物理上限 `WIN_MAX = 11`），选音按「窗宽在外、质量在内」——
  先找能铺满的最小窗，再在窗内按接续 / 跳进 / 回退三档质量挑一个。
- **3-1-3 / 2-1-2 的两个「1 音弦」**落在窗内相对最低品 `+1` / `+2`，即中指 / 无名指的位置。

### 4. 七弦定弦下的 3NPS / 4NPS 指型显示

- **统一成「1–7 弦组」**。这两条指型的骨架原本只写 6 项，在七弦定弦上会平移出
  「2–7 弦组」与「1–6 弦组」两组把位 —— 同一张图重复印两遍、各少一根弦。
  现在骨架按弦数补长（每弦取音数不变），只出一组铺满 1–7 弦。
- 代价：**7 弦 C# 定弦（`C#F#BEG#C#C#`）上 4NPS 铺不出来**。该定弦 7 根弦只跨 2 个八度、
  最上面两根同音，而 7 弦宽的 4NPS 要 28 个音 ≈ 4 个八度，24 品琴颈放不下 ——
  页面会提示「放不下该指型」（统一前的卡片是靠 6 根弦铺出来的）。

### 5. 五声音阶新增「宽五声」，替换不可用的五声音阶 4NPS

- 大调 / 小调五声的「**4NPS**」一档换成「**宽五声（一弦三音 · 本把位 + 右邻把位）**」。
  换掉它的原因：原来的 4NPS 在这两条音阶上**数学上就铺不出来** ——
  5 音音阶 × 每弦 4 音 × 6 弦 = 24 音 ≈ 4.8 个八度，24 品琴颈装不下
  （实测：大调五声 **0 块**，小调五声在 17 条定弦 × 全部音阶 × 全部指型里也只剩 **2 块**）。
- **它是什么**：五声把位是「每弦 2 音」，相邻两个把位在每根弦上恰好共享一个**边界音**
  （本把位每弦的高音 = 右邻把位同弦的低音），并起来正好每弦 3 音 ——
  这就是五声的「一弦三音」型：食指起、小指落，
  指序随音程关系取 `1-3-4` / `1-2-4` / `1-2-3`。
- **不规则定弦上的兜底**：弦间音程不是纯四度的定弦（DADGAD / Open G / 七弦 Drop A）上，
  相邻两把位在某根弦上会共享 2 个音（并集只剩 2 音）或 0 个（并集变成 4 音）。
  这时**不把整块丢掉**，改为从「本把位每弦的最低音」起取 3 个连续音阶音；
  在规则定弦上这与并集**逐音相同**。不加这条兜底，七弦 Drop A 的小调五声会**一块都铺不出来**。
- 选音阶时这一档会**自动换过来**（下拉随音阶换档，同一时刻仍显示 9 项），
  存档里遗留的旧档位也会被纠正。
- 布鲁斯族虽然同属五声族、4NPS 同样铺不出来，但音数不是 5、
  「两个相邻把位共享一个边界音」不成立，所以不开这一档。

### 文档

- `docs/数据与算法.md`：指型表补 `pentwide` 一行（9 种 → 10 种）；新增
  「宽五声：大小调五声的 4NPS 替换」一节（定义、兜底铺法、17 条定弦的实测块数）；
  新增「七弦定弦：3NPS / 4NPS 铺满 1–7 弦」一节；
  补正第 39 轮的窗宽目标（6 品 → 7 品，此前本文件漏改）并更新三个探针的实测数据；
  改写「音高与拼写」一节（和弦构成音的命名规则）；补正 CAGED 推导指型的描述；
  回归不变量补一条。
- `docs/功能说明.md`：指型下拉补「宽五声」及其自动换档说明；指型规则补一条七弦说明。
- `README.md` / `README.en.md`：指型系统 9 → 10 种。

### 技术

- 单文件、零依赖、无构建，`index.html` **688,370 字节**，
  SHA256 `96e9f8f3521c5c0b89944d543bc8cf18348d6969858935c29789ba4560dd2829`。

## [1.0.0] — 2026-09-14

首次公开发布。单文件应用，`index.html` 双击即用。

### 十一个栏目

- **指板探索**：音阶 / 琶音在指板上的高亮，9 种指型系统、按把位分屏、色块叠加、特征和弦琶音高亮、
  调式色彩档案与色彩光谱
- **练习模式**：「找音」条件反射训练 + 「音阶听辨」（8 组母音阶体系分组出题）
- **音阶库**：81 条音阶 / 琶音，9 大体系，含音级结构、构成音、特征和弦、特点与用法
- **CAGED 和弦**：五大指型位置窗，按实际品位从低到高排列
- **音阶和声速查**：七级数 × 五层扩展和弦表
- **琶音探索**：琶音在 CAGED 五个把位的形状与常配音阶
- **连接训练**：456 条和弦进行的逐音声部连接
- **玩点即兴**：200 首伴奏带 —— 进行铺开、声部连接最优按法、逐和弦即兴素材、把位指板图
- **即兴查询表**：给定和弦性质与调性列出可用音阶与替代方案
- **和弦配置**：整条进行的全局最优按法搜索（多方案）
- **2-1-2 琶音**：跨弦 2-1-2 交替的琶音指型一览

### 数据

- 音阶 / 琶音 **81** 条，调式色彩档案 **58** 条
- 定弦 **17** 种（6 弦吉他 10、7 弦吉他 5、4 弦贝斯 1、5 弦贝斯 1）
- 和弦性质 **23** 种，和弦进行 **456** 条，伴奏带 **200** 首
- 弦组 **16** 组，听辨母音阶体系 **8** 组

### 算法与正确性

- **声部连接代价按「品位几何」计算**，不按音高差：`Σ[|Δ品| + 0.35×|Δ半音 − Δ品|] + 1.0×|手位中心位移| + 3.4×声部数不齐`
  - 100 首伴奏带 / 1000 个相邻和弦对实测：手位移动中位 **0.5 品**、最大 **2.0 品**
- **CAGED 五型 = 位置窗**（锚在特定弦的根音上，只跟主音走、与音阶无关），显示次序按实际最低音品升序
- **母音阶 / 子音阶分离**，调式按级数排序；听辨干扰项剔掉音级结构相同的音阶
- **把位是纵向概念**：同一把位内跨弦铺开的一整条音型
- **2-1-2 指型** 块内取音是三度叠置的琶音，不是连续音阶音；起始弦可在 6 / 5 / 4 / 3 弦切换，弦数不够自然截断
- **玩点即兴的坐标系统一**：和弦卡角标、素材表「本方案把位」列、把位指板图标题取同一个把位窗口变量

### 技术

- 单文件、零依赖、无构建、无 CDN；页面不发出任何网络请求
- 声音用 Web Audio 三角波 + 锯齿波实时合成，**仓库不含任何音频文件**
- 指板图是运行时计算的 SVG，非位图
- 浅色 / 暗色双主题；偏好写 `localStorage`

### 已知边界

- 仅面向 6 / 7 弦吉他、4 / 5 弦贝斯，十二平均律
- 声部连接为品位几何近似，不含生理层面的指法限制
- 个别伴奏带在原素材中只有整首的音阶选项，素材表会回退到整首通用项并以虚线徽章标注
